manifest_version = 1
id = "linux.user-temp"
platform = "linux"
category = "system"
name = "User temporary files"
safety_level = "safe"
restore_hint = "Temporary files owned by the current user."

[[targets]]
kind = "template"
value = "%TMPDIR%"

[[targets]]
kind = "template"
value = "%TEMP%"

[provenance]
source = "owned"
license = "project-owned"
notes = "Initial owned Linux catalog entry based on conventional user temporary directory environment variables. Rebecca does not target /tmp directly."
