# Engineering Memory Update Log

## 2026-07-03
* **Inspect-map dogfood report**: Added `scripts/dogfood/run-inspect-map-report.ps1` plus `scripts/dogfood/README.md` so backend evidence for `inspect map` is collected as one JSON scan per requested backend/repetition and then derived into `inspect-map-report.json`, `inspect-map-runs.csv`, `inspect-map-rows.csv`, and `inspect-map-summary.md`. The script refuses implicit roots and drive roots unless explicitly allowed, isolates Rebecca config/state/cache/history under the report directory, and includes `-SelfTest` coverage for parser/report derivation. Local dogfood on `docs` wrote `target/inspect-map-dogfood/20260703-162646-54696`: portable-recursive, windows-native, and requested experimental NTFS/MFT all matched at 975297 logical bytes, 91 files, and 15 directories, with the experimental request recording portable fallback rather than claiming exact MFT coverage.
* **Disk-map cleanup advice**: Added `inspect map --cleanup-advice` and `--advice-status cleanable|maybe-cleanable|contains-cleanable|protected|unknown` so ranked disk-map entries can carry read-only cleanup guidance from the rule catalog, project artifact policy, and protection policy. Advice lives on `DiskMapEntry`, uses protection-first matching, distinguishes direct cleanability from parent directories that merely contain cleanable targets, exposes required opt-ins/warning gates/age gates as facts, and only suggests dry-run `clean --rule` or `purge --artifact` commands. Added entry filters with `--min-logical-bytes`, `--entry-kind`, and `--path-contains`, updated the CLI API schema/table output, and verified focused core, CLI, and API schema coverage.
* **Disk-map table row filtering**: Added repeated `inspect map --table-row total|root|entry|group` for CSV/TSV table exports. The default `--table csv|tsv` output remains the full `total`, `root`, `entry`, and `group` table, while scripts can now request only the row kinds they need without parsing and filtering unrelated rows.
* **Disk-map CSV/TSV table export**: Added `inspect map --table csv|tsv` for flat spreadsheet/query-tool output. The export writes `total`, `root`, `entry`, and `group` rows with a single header, keeps JSON/NDJSON v1 envelopes unchanged, and rejects mixing table output with `--format json` or `--format ndjson`.
* **Disk-map NDJSON row stream**: `inspect map --format ndjson` now emits bounded `map-entry` events for ranked `top_entries` and `map-group` events for requested distribution groups before the terminal `completed` event. The row events use v1 payload kinds `inspect-map-entry` and `inspect-map-group`, while the final `completed` event still carries the authoritative `inspect-map` report for existing consumers that read only the terminal payload.
* **Disk-map sortable rankings**: Added `inspect map --sort logical|allocated|files|unique` for top entries and `--group-sort logical|allocated|files|unique` for grouped distributions. Sorting is request-level, shared by portable, Windows native, and experimental NTFS/MFT backends, and falls back to logical-byte rank when allocated or unique metrics are unavailable.
* **Disk-map grouped distribution views**: Added repeated `inspect map --group-by extension|depth|age` plus `--group-limit`. Groups are file-only distribution summaries produced during the same traversal as ranked entries and reuse the disk-map logical, allocated, and unique-byte metrics. Windows native and experimental NTFS/MFT disk-map inventory now feed the same grouped report model without requiring a second scan.
* **Windows-native disk-map unique file accounting**: Added nullable `unique_logical_bytes` and `unique_allocated_bytes` to disk-map metrics and entries. The Windows native backend now captures `(volume serial, file index)` from read-attribute handles and deduplicates hardlinked paths for the unique fields while preserving path-ranked logical and allocated totals.
* **Windows-native disk-map filesystem semantics**: Added `estimate_caveats` for native disk-map compressed files, sparse files, hardlinked files, and skipped reparse points. Native hardlinks are identified through read-attribute handles and caveated rather than silently treated as unique physical usage; regular native scans keep the fast find-data path and only `inspect map` asks for the extra allocation/semantics metadata.
* **Windows-native allocated disk-map bytes**: Extended `inspect map --scan-backend windows-native` so file entries carry Windows-reported allocation bytes when available. Directory and root totals now aggregate those file allocation values while retaining nullable `allocated_bytes` whenever any file allocation cannot be read, keeping logical-byte totals exact and allocation reporting honest.
* **Windows-native disk-map inventory**: Refactored `inspect map --scan-backend windows-native` to use the real Windows find-data inventory path instead of portable fallback on supported local roots. The shared disk-map walker now accepts entry metadata from native enumeration, preserving bounded diagnostics, max-depth ranking, and exact provenance. Local elevated `inspect-map` dogfood on `docs/plans` under `target/ntfs-dogfood/20260703-042022-86200/` matched portable-recursive at 654253 bytes and 42 files; `windows-native` was the fastest actual backend at 1706 ms versus 22451 ms for portable and 5962 ms for targeted NTFS/MFT.
* **NTFS dogfood backend comparisons**: Upgraded `scripts/ntfs/run-live-mft-dogfood.ps1` so reports include top-level `comparisons` grouped by mode/root, with portable-baseline match status, fastest run id, fastest requested/actual backend identity, duration ratios, metric deltas, and extracted `diagnostic_summary` totals. Local elevated `inspect-map` dogfood on `docs/plans` under `target/ntfs-dogfood/20260703-032731-100244/` matched portable-recursive across portable, Windows-native requested fallback, and `windows-ntfs-mft-experimental-targeted-fsctl`: 654253 bytes, 42 files, zero deltas. The fastest run was requested as `windows-native` but actually used `portable-recursive`, validating the split fastest requested/actual backend fields.
* **Inspect-space bounded diagnostics**: Aligned `inspect space` with disk-map diagnostics by adding complete `diagnostic_summary` counts, bounded raw samples, and `--diagnostic-limit`. Human, JSON, and NDJSON outputs now use the same summary-first contract as `inspect map`, so root-level and scan-failure diagnostics cannot flood machine output while authoritative counts remain visible.
* **Disk-map bounded diagnostics**: Added a bounded `inspect map` diagnostic collector with complete `diagnostic_summary` counts and raw diagnostic samples capped by `--diagnostic-limit`. Root and fallback diagnostics are priority samples, while child-level diagnostic storms retain grouped counts without flooding human, JSON, or NDJSON output. Human disk-map output now prints grouped diagnostic counts before bounded samples.

## 2026-07-02
* **Disk-map partial diagnostics**: Hardened portable `inspect map` so child-level metadata failures, unreadable child directories, directory-entry iteration errors, and child reparse points produce explicit diagnostics while the report continues with conservative zero-byte skipped subtrees. Root-level missing or unreadable roots remain skipped roots, and cancellation still propagates as cancellation. Added a small internal filesystem walker seam for deterministic tests without relying on Windows ACLs or timing races.
* **Portable disk-map streaming traversal**: Refactored the default portable `inspect map` path from full `PortableDiskMapNode` tree materialization to streaming post-order aggregation. Directory metrics are computed after children and pushed directly into the bounded `DiskMapTopEntries` heap, so memory stays tied to traversal depth and `--top` rather than the full rendered tree. Added root-file coverage to keep depth-0 file roots stable while existing ranking, top-zero, max-depth, missing-root, and fallback tests continue to cover the public contract.
* **NTFS adaptive disk-map backend**: Refactored `inspect map --scan-backend windows-ntfs-mft-experimental` so scoped roots use targeted per-record FSCTL traversal and `$I30` expansion instead of building a full-volume `MftIndex`. Drive roots and explicit `REBECCA_NTFS_MFT_FULL_INDEX_FALLBACK=1` diagnostics retain the full-index path. The targeted collector now produces disk-map totals, bounded top entries, allocated bytes when available, max-depth rendering, duplicate-record caveats, and the same `windows-ntfs-mft-experimental-targeted-fsctl` source provenance used by ordinary estimates. Local elevated dogfood under `target/ntfs-dogfood/20260702-185357-58228/` completed `docs/plans` with 624422 logical bytes, 39 files, 910 ms, no fallback, and targeted source provenance.
* **Read-only disk map inspect surface**: Added `rebecca inspect map` with bounded ranked entries, `logical_bytes`, nullable `allocated_bytes`, max-depth rendering, human/JSON/NDJSON API v1 output, CLI/schema/examples/docs, and a report-only `rebecca-core::disk_map` model. `MftIndex` now preserves unnamed `$DATA` allocated size and exposes child-entry traversal so the explicit NTFS/MFT disk-map path can walk a full-volume index without changing ordinary targeted cleanup estimates. The default map backend remains portable recursive because local elevated dogfood on E: showed full-volume MFT construction exceeding the 20 second internal budget and a no-budget 180 second script timeout; explicit `--scan-backend windows-ntfs-mft-experimental` records fallback provenance instead of returning partial exact maps.
* **NTFS targeted MFT traversal**: Replaced the ordinary live experimental NTFS/MFT estimate path with target-root-scoped per-record FSCTL traversal. The backend now reads the full target file reference, lazily expands direct `$ATTRIBUTE_LIST` extension records through `rebecca-ntfs`, traverses trusted `$I30` children with sequence/parent/budget checks, treats traversal budget exhaustion as fallback-capable instead of exact partial data, and keeps the older full-volume `MftIndex` builder as an explicit diagnostic fallback behind `REBECCA_NTFS_MFT_FULL_INDEX_FALLBACK=1`. The parser now accepts both raw `$MFT` records and `FSCTL_GET_NTFS_FILE_RECORD` records whose update-sequence fixups are already applied. Elevated dogfood on `docs/plans` showed `windows-ntfs-mft-experimental-targeted-fsctl` matching portable-recursive at 590311 bytes and 37 files with no fallback.
* **NTFS parallel MFT chunk parsing**: Changed the live sequential `$MFT` source to read 8 MiB chunks and parse bounded chunk windows on a dedicated MFT parse pool. The new helper preserves chunk order, base record ids, cancellation, source fallback, and build-stage timing. Local elevated dogfood reduced `sequential-parse-records` to roughly `1842ms`, but the default 20 second budget still falls back because full-volume raw `$MFT` reads dominate (`sequential-read-mft-bytes` roughly `17253ms` on the latest run, with longer times under E: drive load). Next NTFS speed work should avoid full-volume indexing for ordinary cleanup targets by traversing from the target root through `$I30` and record references.
* **NTFS live MFT stage timings**: Added `NtfsMftBuildMonitor` around the experimental live NTFS/MFT backend so timeouts name the active build stage and include completed timing slices such as volume open, volume data, retrieval pointers, sequential raw MFT byte reads, sequential record parsing, FSCTL read/parse, index-allocation expansion, and `MftIndex` build. Successful experimental runs can emit an opt-in `mft-index-build-timing` caveat with `REBECCA_NTFS_MFT_INDEX_TIMINGS=1`, while default machine output stays quiet. Updated README, configuration, release, performance docs, changelog, and current-state memory.
* **NTFS sequential read/parse split timing**: Split the sequential live `$MFT` source timing from a combined read/parse stage into `sequential-read-mft-bytes` and `sequential-parse-records`. Local elevated dogfood now shows the 20 second budget is consumed by both raw volume reads and record parsing, not by volume open or retrieval pointer setup, making chunk-level parallel parsing or a read/parse pipeline the next most grounded optimization target.
* **NTFS live MFT build budget**: Added a bounded live-index construction guard to `windows-ntfs-mft-experimental`. The backend now carries a default 20 second `NtfsMftBuildBudget` through sequential `$MFT` chunk reads, per-record FSCTL loops, and stream-backed `$INDEX_ALLOCATION:$I30` reads, while preserving explicit cancellation as `OperationCancelled`. Budget expiry returns a fallback-capable `PlatformUnavailable` error with `REBECCA_NTFS_MFT_INDEX_TIMEOUT_SECONDS` guidance, and the per-command volume cache now single-flights live index builds plus caches unavailable outcomes so multiple targets do not repeat the same failed volume attempt. Updated README, configuration, release, performance docs, changelog, and current-state memory. Verified with targeted `rebecca-core` check and Windows MFT backend tests during this slice.
* **NTFS index-allocation stream reader**: Completed the stream-backed large-directory refactor for `rebecca-ntfs` and the live experimental backend. Parser records now preserve generic nonresident attribute streams, `$INDEX_ROOT` index-record metadata, validated INDX entries, direct `$ATTRIBUTE_LIST` `$INDEX_ALLOCATION` extensions, and bounded `invalid-index-allocation` caveats. `MftIndex` can build from an expanded `NtfsRecordSet`, so valid nonresident `$INDEX_ALLOCATION:$I30` entries can repair stale parent maps without changing logical-byte semantics. `rebecca-core` now supplies raw live volume reads through `NtfsStreamSource` while preserving cancellation, source labels, cache reuse, and fallback. Added deterministic fixtures, parser microbench coverage for stream-backed expansion and fragmented runlists, fixture provenance notes, docs, ADR, README, API, and changelog updates. Verified with `cargo fmt --all --check`, `cargo check --workspace`, `cargo nextest run --workspace` (569 tests), `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo deny check`, `cargo check -p rebecca-core --benches`, `cargo check -p rebecca-ntfs --benches`, `cargo bench -p rebecca-ntfs --bench mft_parser -- --test`, `pwsh -File scripts/perf/run-benchmark-matrix.ps1`, and `git diff --check`.
* **NTFS parser core dependency gate**: Completed the fearless first-party `rebecca-ntfs` parser path behind owned DTOs after the dependency gate, then replaced the flat MFT model with sequence-aware file references, attributes, unnamed/named data streams, data runs, direct attribute-list extension resolution, hardlink path candidates, resident `$I30` directory-index fallback, and logical/allocated/initialized stream metadata. The live experimental backend still owns privilege, volume identity, source provenance, cache reuse, fallback, and deletion non-authority; CLI/core tests disable live MFT reads in debug/test isolation so elevated developer shells do not scan whole volumes. Known unsupported edges at that slice remained caveated or fallback-only and were intentionally addressed by the later index-allocation stream reader work. Verified with `cargo fmt --all --check`, `cargo check --workspace`, `cargo nextest run --workspace` (553 tests), `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo deny check`, `cargo check -p rebecca-core --benches`, `cargo check -p rebecca-ntfs --benches`, `pwsh -File scripts/perf/run-benchmark-matrix.ps1`, and `git diff --check`.
* **NTFS sequential MFT reader**: Upgraded `windows-ntfs-mft-experimental` to try a read-only sequential `$MFT::$DATA` source before the per-record FSCTL source, parse base-record-aware batches, preserve FSCTL and directory-scanner fallbacks, carry `estimate_backend_source` through fresh scans and scan-cache hits, and add an opt-in live NTFS source benchmark via `REBECCA_PERF_MATRIX_LIVE_NTFS=1`. Updated CLI schema/docs, README, configuration, release dogfood, performance docs, changelog, ADR, and current-state memory. Verified with `cargo fmt --all --check`, targeted core/CLI suites, `cargo nextest run --workspace` (539 tests), `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo deny check`, `cargo check -p rebecca-core --benches`, `pwsh -File scripts/perf/run-benchmark-matrix.ps1`, `git diff --check`, and unelevated dogfood logs under `target/dogfood/`.
* **NTFS live volume index experiment**: Turned `windows-ntfs-mft-experimental` from a stub selector into an opt-in read-only Windows NTFS/MFT indexing backend with per-command volume-index reuse, `rebecca-ntfs` subtree aggregation, parser/ambiguity caveats, fallback provenance, and docs/release dogfood coverage. Removed the obsolete `rebecca-windows` stub module. On this machine, dogfood recorded permission-denied live-volume access and safe fallback to `windows-native` with `experimental-ntfs-mft-fallback` caveats. Verified with `cargo fmt --all --check`, `cargo nextest run --workspace` (528 tests), `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo deny check`, `cargo check -p rebecca-core --benches`, `pwsh -File scripts/perf/run-benchmark-matrix.ps1`, `git diff --check`, and dogfood logs under `target/dogfood/`.
* **Estimate provenance explainability**: Added additive v1 estimate provenance across cleanup, purge, and `inspect space` while keeping `estimate_source` stable. Planner and inspect paths now propagate backend, confidence, fallback reason, and caveats from fresh scans and scan-cache hits; `inspect space --scan-backend` shares cleanup backend selectors; human output summarizes cache/fallback/caveat details without making backend metadata deletion authority. Updated API schema/examples, README, configuration, release notes, changelog, and dogfood logs. Verified with `cargo fmt --all --check`, `cargo nextest run --workspace` (523 tests), `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo deny check`, `git diff --check`, and dogfood logs under `target/dogfood/`.

## 2026-07-01
* **Cleaner engine U15 release and dogfood gates**: Accepted the deletion/recovery ADR as the implemented cleanup execution model, tightened release documentation around performance matrix, JSON backend dogfood, dry-run delete smoke, release packaging, SBOM, and checksum preflight, and closed the performance-engine plan with final verification. Verified with `cargo fmt --all --check`, `cargo nextest run --workspace` (520 tests), `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo deny check`, `pwsh -File scripts/perf/run-benchmark-matrix.ps1`, release smoke artifact/SBOM/checksum scripts, and dogfood logs under `target/dogfood/`.
* **Cleaner engine U10-U14 Windows fast paths and rule governance**: Added opt-in Windows native directory scanning with portable fallback, introduced a read-only `rebecca-ntfs` parser crate and generated-record benchmark, exposed the experimental `windows-ntfs-mft-experimental` selector with caveats rather than live-volume authority, added a conservative USN Journal scan-cache validation model, reset the pre-release scan-cache record number to the single v1 identity contract, and governed cleanup rule search semantics through `search_kind`, catalog output, and a per-plan glob discovery index. Updated README, configuration, API schema, rule authoring, release dogfood guidance, ADRs, and changelog. Targeted verification passed for scan engine, scan cache, discovery, manifest, Windows adapter, NTFS parser, CLI clean/catalog, built-in rules, benches compile, formatting, and diff hygiene.
* **Cleaner engine U8-U9 batch recycle execution**: Added an optional batch-delete contract to `CleanupBackend` so the parallel executor can hand already revalidated, non-overlapping target batches to capable backends while preserving per-target outcomes and legacy per-target parallelism for ordinary backends. Updated the Windows Recycle Bin backend to batch safe targets through `trash::delete_all`/Shell `IFileOperation`, expand preserve-root targets into direct child entries, and reconstruct or fall back per target after batch errors. Expanded executor contract tests, Windows recycle-bin tests, the performance matrix, deletion/recovery ADR, README, configuration docs, and Unreleased changelog. Verified with `cargo check -p rebecca-core --benches`, `cargo nextest run -p rebecca-core --test executor_contract`, `cargo nextest run -p rebecca-windows --test recycle_bin`, `cargo nextest run -p rebecca --test cli_clean --test cli_purge --test cli_apps`, `cargo fmt --all --check`, `git diff --check`, `cargo check -p rebecca`, and `pwsh -File scripts/perf/run-benchmark-matrix.ps1`.
* **Cleaner engine U7 scan-cache identity**: Reset the pre-release scan-cache record number to compact v1 JSON with scan backend, estimate confidence, optional filesystem identity fields, and USN checkpoint placeholders. The earlier experimental numbering was removed because no released consumers depend on it. Default cache writes now use atomic replacement without strict file sync on the hot path, while strict sync remains available as an internal policy option. Verified with scan-cache and planner regression tests during the U7 slice.
* **Cleaner engine U5 progress/API contract**: Added target-level default NDJSON progress with `--progress-detail file` opt-in for ordinary cleanup file events, throttled human file progress refreshes, and expanded the performance matrix with a target-progress rule-plan scenario. Collapsed the pre-release CLI machine API back to a single `rebecca.cli.v1` contract by deleting the v2 namespace, moving catalog/inspect schemas and examples into `docs/api/cli/v1`, and removing v2-only code/test helpers.
* **Cleaner engine scan/planner U2-U4**: Split scan measurement behind an internal backend contract with portable-recursive metadata, exact confidence, cancellation-aware progress plumbing, and no direct production `ignore::WalkBuilder` callers. Refactored ordinary rule planning into staged rule candidates, safety/dedupe assessment, bounded parallel measurement through the shared scan pool, stable progress replay, and deterministic final targets. Added planner characterization coverage for duplicate paths and stable progress ordering, and expanded the performance matrix with `rule_plan_32_dirs_1024_files`. Verified with `cargo fmt --all --check`, `cargo nextest run -p rebecca-core --test scan_engine`, `cargo nextest run -p rebecca-core --test planner`, `cargo nextest run -p rebecca --test cli_clean`, `cargo check -p rebecca-core --benches`, `git diff --check`, and `pwsh -File scripts/perf/run-benchmark-matrix.ps1`.
* **Cleaner performance matrix U1**: Added `crates/rebecca-core/benches/perf_matrix.rs` with deterministic Criterion scenarios for many-small-file scanning, file-level progress overhead, one large flat directory, a deep tree, parallel target measurement, duplicate target candidates, scan-cache miss/store, scan-cache hit, serial deletion, and parallel deletion. Added `scripts/perf/run-benchmark-matrix.ps1` to run the matrix and assemble Criterion estimates plus scenario metadata into `target/perf/rebecca-core-perf_matrix-report.json`, and documented the workflow in `docs/performance/perf-matrix.md`. Verified with `cargo check -p rebecca-core --benches`, `pwsh -File scripts/perf/run-benchmark-matrix.ps1`, and `pwsh -File scripts/perf/run-benchmark-matrix.ps1 -SkipRun`.

## 2026-06-30
* **Cleanup performance/API hardening refactor**: Completed the fearless hardening plan by centralizing command-aware CLI API contracts, moving v2 fatal JSON/NDJSON errors onto the v2 envelope, making `cache purge --yes` recoverable by default with explicit `--permanent`, pushing project-artifact reclaim limits before lower-ranked measurement, deleting buffered file-progress replay from parallel measurement, bounding `inspect space` and `inspect lint` top sections, reusing walker file-type metadata in scan traversal, and routing `history --limit` through bounded tail loading. Updated `CHANGELOG.md`, README/configuration/API docs, and current-state memory. Verified with `cargo fmt --all --check`, targeted CLI/core nextest suites, `cargo nextest run --workspace` (444 tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.
* **Cleanup workflow architecture refactor**: Completed the fearless cleanup-workflow architecture plan by making CLI workflow output contracts explicit, centralizing runtime cancellation and Ctrl+C handling, splitting planner/project-artifact modules, surfacing project-artifact discovery diagnostics, adding target estimate provenance, extracting human renderers from machine transport, and adding read-only `purge inspect` with the later-canonical `inspect-artifacts` API payload. Refreshed CLI API schemas/examples, README, configuration docs, and current-state memory. Verified with `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, targeted core/CLI/output nextest suites, `cargo nextest run --workspace` (383 tests), and `git diff --check`.

## 2026-06-27
* **Domestic desktop-app cache batch**: Added conservative Windows cache rules for WeChat, Enterprise WeChat, QQ, Feishu, DingTalk, WPS, Baidu Netdisk, Tencent Meeting, QQ Music, and Tencent Video. Tightened `ProtectionPolicy` with app-specific cache allowlists and durable-state blockers, expanded safety-policy coverage for both positive cache leaves and nearby account/sync/session data, updated the builtin catalog and shared builtin-rule ID expectations, and refreshed the README, rule-authoring guidance, safety audit, and rule-source index to describe the new cache-vs-state boundary. Verified with `cargo fmt --all`, `cargo nextest run -p rebecca-core --test safety_policy`, `cargo nextest run -p rebecca-rules`, `cargo nextest run -p rebecca-cli --test cli_scan -p rebecca-cli --test scan`, `cargo nextest run -p rebecca-cli --test cli_clean`, `cargo nextest run --workspace` (314 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.

## 2026-06-28
* **CLI API v1 contract slice**: Replaced command-local JSON flags with global `--format human|json|ndjson`, wrapped machine successes in `rebecca.cli.v1` envelopes, added structured machine error envelopes, emitted NDJSON cleanup lifecycle/progress/completion/error events, exposed `doctor permissions` as a structured diagnostic payload, and published Draft 2020-12 schema/docs/examples under `docs/api/cli/v1`. Verified with `cargo check -p rebecca-cli`, `cargo nextest run -p rebecca-cli --test cli_api -p rebecca-cli --test cli_output -p rebecca-cli --test cli_help -p rebecca-cli --test cli_completion`, `cargo nextest run --workspace` (359 tests), `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.
* **Cleanup-core execution classification**: Added a stable `execution-target-missing` reason code for targets that disappear between planning and execution, kept protected execution-time targets on `safety-policy-blocked`, kept backend permission/IO failures on `execution-failed`, and refreshed the README, safety audit, and current-state memory so the documented cleanup-core hardening contract matches the implementation. Verified with `cargo fmt --all`, `cargo nextest run -p rebecca-core --test executor_contract -p rebecca-core --test history -p rebecca-cli --test cli_history`, `cargo nextest run -p rebecca-core --test safety_policy -p rebecca-core --test scan_engine -p rebecca-core --test planner -p rebecca-cli --test cli_clean`, and `git diff --check`.
* **Scan-cache lifecycle sweep**: Added a best-effort scan-cache prune pass that runs after plan builds, removes stale/corrupted/expired records from the cache dir without changing per-target miss labels, and covered the behavior in `rebecca-core` planner and scan-cache regressions. Verified with `cargo fmt --all`, `cargo nextest run -p rebecca-core`, `cargo nextest run -p rebecca-cli --test cli_clean -p rebecca-cli --test cli_output`, and `cargo nextest run --workspace`.
* **Cleanup execution parallelism and batch recycle-bin deletes**: Added bounded parallel cleanup execution in `rebecca-core` so allowed targets are revalidated once, grouped into non-overlapping batches, and dispatched on a shared rayon pool; wired the CLI to that parallel executor; changed the Windows recycle-bin backend to call `trash::delete_all` for directory targets so their direct children move in one batch; and added concurrency and recycle-bin regression coverage plus a deletion benchmark slice. Verified with `cargo check -p rebecca-core --benches`, `cargo nextest run -p rebecca-core --test executor_contract -p rebecca-windows --test recycle_bin -p rebecca-cli --test cli_clean`, `cargo nextest run --workspace`, and `cargo clippy --workspace --all-targets -- -D warnings`.

## 2026-06-29
* **Scan engine deep-module refactor**: Added `docs/plans/2026-06-29-001-refactor-scan-engine-deep-module-plan.md`, introduced `ScanEngine` as the caller-facing cleanup measurement module, moved `ignore::WalkBuilder` behind a private cleanup traversal adapter, and made cleanup measurement count entries hidden or ignored by search-tool filters such as `.ignore` and `.gitignore`. Verified with `cargo check -p rebecca-core --benches`, `cargo nextest run -p rebecca-core --test scan_engine`, `cargo nextest run -p rebecca-core --test planner -p rebecca-core cache`, `cargo nextest run -p rebecca-cli --test cli_clean -p rebecca-cli --test cli_output`, `cargo nextest run --workspace` (362 tests), `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `git diff --check`, and engineering wiki memory validation.

## 2026-06-23
* **Initialization**: Created engineering wiki memory bundle.
* **Documentation baseline**: Collected reference projects and started Windows-first ADR drafting for the Rust CLI cleaner.
* **ADR baseline**: Added platform strategy and core runtime architecture ADRs; validated the engineering memory bundle.
* **Boundary ADRs**: Added workspace, Windows privilege/registry, and scan engine ADRs; updated memory bundle state.
* **Safety and state ADRs**: Added deletion/recovery, rule provenance, and configuration/local-state ADRs.
* **Workspace bootstrap**: Initialized the `rebecca` Rust workspace and verified it with `cargo fmt`, `cargo check --workspace`, and `cargo nextest run --workspace`.
* **Cleanup-core hardening**: Hardened the existing Windows cleanup core by bounding planner-side parallel scans with a shared scan thread pool, pruning stale/corrupted scan-cache files on lookup, and refreshing the mature-contract docs/current-state memory. Verified with `cargo fmt --all`, `cargo nextest run -p rebecca-core --test scan_engine -p rebecca-core --test planner`, and `cargo nextest run --workspace` smoke coverage on the touched tests.
* **State layout refinement**: Split local state and cache into distinct `state/` and `cache/` subdirectories under `%LOCALAPPDATA%\\Rebecca`.
* **MVP implementation plan**: Added `docs/plans/2026-06-23-001-feat-windows-cleanup-mvp-plan.md` to define the first Windows cleanup loop: owned rules, path expansion, safety policy, parallel scan, Recycle Bin execution, history, CLI output, and tests.
* **Windows cleanup MVP implementation**: Implemented the plan-first cleanup loop with typed rule targets, environment path expansion, safety policy, scanner, planner, execution backend contract, Windows Recycle Bin backend, history JSONL store, CLI commands, README, and 31 passing tests.
* **MVP commit**: Committed the Windows cleanup MVP with subject `feat: build windows cleanup mvp`; post-commit verification passed with `cargo fmt --all -- --check`, `cargo check --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace`.
* **Overlapping template dedupe fix**: Fixed planner deduplication so overlapping templates such as `%TEMP%` and `%LOCALAPPDATA%\\Temp` do not double-count the same path. Added regression tests and re-verified with 33 passing tests plus real dry-run smoke runs for `system`, `browser`, and `development --allow-moderate`.
* **TOML rule catalog**: Moved the three built-in Windows rules from Rust constructors into TOML files under `crates/rebecca-rules/rules/windows/`; added strict TOML schema parsing, duplicate target spec validation, documentation updates, and 37 passing workspace tests.
* **First rule expansion batch**: Added `docs/rule-authoring.md` and expanded the TOML-backed catalog from 3 to 8 rules with Chrome cache, DirectX shader cache, pip cache, VS Code cache, and Windows Error Reporting data. Re-verified with 38 passing workspace tests, `cargo run -p rebecca-cli -- scan`, and a dry-run smoke over the five new rules.
* **Glob target discovery**: Added `glob-template` target specs, bounded wildcard path discovery, planner expansion for multi-path targets, Firefox profile cache and Windows thumbnail/icon cache rules, and rule authoring guidance. Re-verified with 43 passing workspace tests, `cargo run -q -p rebecca-cli -- scan`, and dry-run smoke over the new glob rules.
* **Rule selection UX**: Added reusable `RuleSelection` semantics, `scan --category` and `scan --rule` filters, grouped human scan output, README examples, and CLI regression tests. Re-verified with 46 passing workspace tests and smoke runs for browser-category and thumbnail-rule scans.
* **Chromium profile rules**: Expanded Chrome and Edge cache rules to cover explicit `Default` cache paths and bounded `Profile *` Cache, Code Cache, and GPUCache discovery. Added planner regression coverage and re-verified with 47 passing workspace tests plus Chrome/Edge dry-run smoke.
* **Dry-run output readability**: Improved human cleanup plan output with byte formatting, a largest-estimated-targets section, and status-grouped target details while preserving JSON output. Added CLI regression coverage and re-verified with 48 passing workspace tests plus Chrome/Edge human dry-run smoke.
* **Target-level scan progress**: Added planner progress events, wired `clean` to an `indicatif` spinner for human output, and added `--no-progress` for quiet logs while keeping JSON output silent. Added core and CLI regression coverage and re-verified with 50 passing workspace tests.
* **JetBrains cache batch**: Added a `windows.jetbrains-cache` rule for bounded JetBrains `caches` directories under `%LOCALAPPDATA%\\JetBrains\\<product><version>`, plus scan and planner coverage. Re-verified with 51 passing workspace tests and JetBrains scan/clean smoke.
* **Cargo cache batch**: Added a `windows.cargo-cache` rule for `%CARGO_HOME%` and default `%USERPROFILE%\\.cargo` cache subdirectories, including `registry/cache`, `registry/index`, `registry/src`, `git/db`, and `git/checkouts`. Added planner coverage for default and custom `CARGO_HOME` layouts plus CLI rule listing coverage, and updated rule-authoring guidance. Re-verified with `cargo fmt --all --check`, `cargo nextest run -p rebecca-rules`, `cargo nextest run -p rebecca-core --test planner`, and `cargo nextest run -p rebecca-cli --test cli_scan`.
* **Discord cache batch**: Added a `windows.discord-cache` rule for Discord Roaming cache directories under `%APPDATA%\\discord`, `%APPDATA%\\discordptb`, and `%APPDATA%\\discordcanary`, covering `Cache`, `Code Cache`, and `GPUCache` only. Added planner coverage for stable and Canary layouts plus CLI rule listing coverage, updated rule-authoring guidance for Electron caches, and re-verified with `cargo fmt --all --check` and `cargo nextest run --workspace` (54 passing tests).
* **File-level progress and cancellation**: Added file-level scan progress events to the core scanner, cancellation tokens for long scans, planner propagation of per-file progress, Ctrl+C cancellation wiring in the CLI, and README guidance for the new behavior. Added core planner/scan regression coverage and re-verified with `cargo fmt --all --check` and `cargo nextest run --workspace` (57 passing tests).
* **Scan performance baseline**: Added structured scan reports, scan failure classification, and a Criterion benchmark baseline for `rebecca-core` scanning 1,024 files with and without file-level progress callbacks. Added core regression coverage for structured scan errors and report counts, and re-verified with `cargo fmt --all --check`, `cargo nextest run --workspace` (60 passing tests), `cargo check -p rebecca-core --benches`, and a shortened benchmark run. Latest benchmark range: `scan_report_1024_files` about 18.6-20.8 ms; `scan_report_with_file_progress_1024_files` about 14.3-14.8 ms.
* **Steam cache batch**: Added a conservative `windows.steam-cache` rule for Steam's client web cache under `%LOCALAPPDATA%\\Steam\\htmlcache\\Default`, covering `Cache`, `Code Cache`, and `GPUCache` only. Added planner coverage to prove the rule ignores Steam `userdata`, `steamapps`, `Local Storage`, and `IndexedDB`, plus CLI rule listing coverage, updated rule-authoring guidance, and re-verified with `cargo fmt --all --check` and `cargo nextest run --workspace` (61 passing tests).
* **Steam discovery base**: Added Steam installation/library discovery plumbing in `rebecca-core` and `rebecca-windows`, including `libraryfolders.vdf` parsing for current and legacy formats, safe relative Steam target kinds, planner support for application discovery injection, and a `doctor steam` CLI command. Added discovery/planner/CLI/recycle-bin regression coverage and re-verified with `cargo fmt --all --check` and `cargo nextest run --workspace` (72 passing tests).
* **Steam library expansion**: Added conservative Steam library download-staging and temp-cache rules for `steamapps\downloading` and `steamapps\temp`, plus planner and CLI regression coverage, rule catalog registration, and README / rule-authoring updates. Re-verified with `cargo clippy --workspace --all-targets --all-features -- -D warnings` and `cargo nextest run --workspace` (83 passing tests).
* **Steam install library cache**: Added a conservative `windows.steam-install-library-cache` rule for Steam's install-root `appcache\\librarycache`, plus planner skip coverage for missing Steam discovery, CLI rule listing coverage, and README / rule-authoring updates. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-rules`, `cargo nextest run -p rebecca-core --test planner`, and `cargo nextest run -p rebecca-cli --test cli_scan`.
* **Steam CLI regression coverage**: Added deterministic CLI regression coverage for Steam discovery by wiring a debug-only discovery override into the `doctor steam` and `clean --dry-run` test paths, then verified the full Steam slice with `cargo fmt --all`, `cargo nextest run -p rebecca-cli --test cli_clean`, `cargo nextest run -p rebecca-cli --test cli_output`, `cargo nextest run -p rebecca-core --test planner`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and `cargo nextest run --workspace` (88 passing tests).
* **Moderate rule allow coverage**: Added a CLI regression that proves `windows.npm-cache` stays skipped without `--allow-moderate` and becomes allowed when the flag is present, then re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-cli --test cli_clean`, `cargo nextest run -p rebecca-core --test planner`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and `cargo nextest run --workspace` (89 passing tests).
* **Safety contract coverage**: Added planner and CLI regressions for `allow_risky` using temporary risky rules so the parser/planner contract is explicit even without a built-in risky catalog entry.
* **README safety example**: Added opt-in `--allow-moderate` and `--allow-risky` usage examples to README so the documented CLI mirrors the tested contract.

## 2026-06-24
* **Shared protection policy and safety audit**: Added `docs/security-audit.md`, introduced `rebecca-core::protection::ProtectionPolicy`, moved `safety.rs` path assessment behind that shared policy, and wired planner target assessment through it. The policy now blocks protected roots, Rebecca-owned storage, browser private data, credentials, VPN/proxy state, AI/coding durable state, cloud-synced data, container/runtime state, startup automation, and application durable data while allowlisting current safe cache targets. Added core and planner regressions and refreshed README, configuration, and rule-authoring docs. Re-verified with `cargo fmt --all -- --check`, `cargo nextest run -p rebecca-core --test safety_policy`, `cargo nextest run -p rebecca-core --test planner`, `cargo nextest run -p rebecca-core`, `cargo nextest run -p rebecca-rules`, `cargo nextest run --workspace` (228 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.
* **Mole-parity safety governance roadmap**: Added `docs/plans/2026-06-24-018-refactor-mole-parity-safety-governance-plan.md` to turn the long-term goal into a phased roadmap across protected paths/categories, planner and executor revalidation, catalog target-shape validation, audit/history surfaces, and guardrailed rule expansion. Updated current-state memory so future work starts with the safety audit baseline and core protection policy. Verified with `git diff --check`.
* **Rule catalog governance hardening**: Tightened catalog validation so invalid rules with empty category, name, or target paths fail centrally, and added built-in-only guardrails requiring Windows platform, `windows.` id prefixes, non-empty restore hints, owned source, and `project-owned` provenance. Updated rule-authoring guidance and re-verified with `cargo nextest run -p rebecca-rules`, `cargo nextest run -p rebecca-core --test model_contract`, `cargo nextest run --workspace` (223 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.
* **Clean storage-boundary enforcement**: Enforced the configuration/local-state contract in `rebecca clean` by passing Rebecca-owned storage entries into the planner and blocking targets that overlap config, state, history, or cache paths. Extracted shared path-overlap logic for planner and cache purge boundary checks, added core and CLI regressions for default/custom app paths, refreshed the configuration contract, and re-verified with `cargo nextest run -p rebecca-core --test planner`, `cargo nextest run -p rebecca-core cache`, `cargo nextest run -p rebecca-cli --test cli_clean`, `cargo nextest run --workspace` (215 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.
* **Execution-time cleanup revalidation**: Added `execute_cleanup_plan_with_policy` so allowed cleanup targets are reassessed by the shared `ProtectionPolicy` immediately before backend deletion. `rebecca clean` now passes Rebecca-owned storage entries into the executor for real cleanup, the executor contract covers protected browser/private data targets, Rebecca-owned storage, and missing-path skips before backend calls, and the safety audit now tracks catalog target-shape validation as the next major gap. Re-verified with `cargo fmt --all -- --check`, `git diff --check`, `cargo nextest run -p rebecca-core --test executor_contract`, `cargo nextest run -p rebecca-cli --test cli_clean`, `cargo nextest run -p rebecca-core`, `cargo nextest run --workspace` (231 passing tests), and `cargo clippy --workspace --all-targets -- -D warnings`.
* **Catalog target-shape validation**: Added `ProtectionPolicy::assess_catalog_target_shape` and shared relative-target shape checks so built-in rules now fail at catalog load time when they point at credentials, browser private data, application durable data, or non-allowlisted Steam install/library relative targets. Routed Steam discovery's relative-path validation through the shared policy, updated rule-authoring guidance and the safety audit, and re-verified with `cargo fmt --all -- --check`, `cargo nextest run -p rebecca-core --test safety_policy`, `cargo nextest run -p rebecca-core --test discovery`, `cargo nextest run -p rebecca-rules`, `cargo nextest run -p rebecca-core`, `cargo nextest run --workspace` (234 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.
* **Plan closure**: Marked the Windows cleanup MVP plan complete and aligned the current-state memory with the finished MVP.
* **Steam expansion plan**: Drafted `docs/plans/2026-06-24-002-feat-steam-cleanup-expansion-plan.md` to add conservative Steam install-root and library-root cleanup coverage.
* **Steam discovery path normalization**: Normalized Steam install and library path keys so trailing separators no longer produce duplicate discoveries, added regression coverage for install-root and libraryfolders parsing, and verified the workspace with `cargo fmt --all`, `cargo nextest run -p rebecca-core --test discovery`, `cargo nextest run -p rebecca-core --test planner`, and `cargo nextest run --workspace` (106 passing tests). Committed as `5948f3c` with subject `fix(core): normalize steam discovery path keys`.
* **Steam discovery path validation**: Tightened Steam discovery so relative or current-directory library entries are ignored, added regression coverage for the parser and relative Steam templates, and re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-core --test discovery`, `cargo nextest run -p rebecca-core --test planner`, and `cargo nextest run --workspace` (108 passing tests). Committed as `e466fab` with subject `fix(core): tighten steam discovery path validation`.
* **Steam discovery fallback hardening**: Centralized Steam discovery loading so unreadable `libraryfolders.vdf` data now falls back to the install root instead of dropping the discovery result, added regression coverage in `rebecca-windows` and `rebecca-cli`, and re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-windows --test recycle_bin`, `cargo nextest run -p rebecca-core --test discovery`, `cargo nextest run -p rebecca-core --test planner`, `cargo nextest run -p rebecca-cli --test info`, and `cargo nextest run --workspace` (111 passing tests). Committed as `05d3fa1` with subject `fix(steam): fall back when discovery data is unreadable`.
* **Steam libraryfolders merge**: Updated Steam installation discovery to read `libraryfolders.vdf` from both `config` and `steamapps`, merge the discovered library roots, and keep the install root usable when one file is missing. Added regression coverage for dual-source discovery, missing-but-readable cases, and CLI output. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-core --test discovery`, `cargo nextest run -p rebecca-core --test planner`, `cargo nextest run -p rebecca-cli --test info`, and `cargo nextest run --workspace` (114 passing tests). Committed as `a150322` with subject `fix(steam): merge Steam libraryfolders locations`.
* **Steam registry fallback hardening**: Added Windows registry fallback discovery for Steam install paths, covering `HKCU\Software\Valve\Steam\SteamPath`, `HKLM\SOFTWARE\Valve\Steam\InstallPath`, `HKLM\SOFTWARE\WOW6432Node\Valve\Steam\InstallPath`, and `HKCR\steam\Shell\Open\Command`, then split command parsing into a pure helper with unit coverage. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-windows --test recycle_bin`, `cargo nextest run -p rebecca-core --test discovery`, `cargo nextest run -p rebecca-cli --test info`, and `cargo nextest run --workspace` (117 passing tests). Committed as `71e2988` with subject `fix(windows): add steam registry fallback`.
* **Steam registry command parsing refinement**: Deepened the Steam registry fallback seam by extracting command-to-install-path parsing into a pure helper, tightening the unquoted-command path detection to avoid `.exe` misfires, and adding targeted unit coverage for quoted and unquoted Steam command strings. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-windows`, `cargo nextest run -p rebecca-core --test discovery`, `cargo nextest run -p rebecca-cli --test info`, and `cargo nextest run --workspace` (120 passing tests). Committed as `839b703` with subject `refactor(windows): deepen steam command parsing`.
* **Steam HKCU SteamExe fallback**: Added a direct `HKCU\Software\Valve\Steam\SteamExe` fallback before the broader legacy registry search, refactored registry string loading into a reusable helper, and kept the command-path seam covered with focused tests. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-windows`, `cargo nextest run -p rebecca-core --test discovery`, `cargo nextest run -p rebecca-cli --test info`, and `cargo nextest run --workspace` (121 passing tests). Committed as `f1553fb9` with subject `fix(windows): add steamexe registry fallback`.
* **Steam registry source table**: Consolidated Steam discovery into a single ordered registry source table covering `SteamPath`, `SteamExe`, `InstallPath`, and `Shell\Open\Command`, keeping the lookup logic deeper and the fallback order explicit. Added a regression that pins the source order and re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-windows`, `cargo nextest run -p rebecca-core --test discovery`, `cargo nextest run -p rebecca-cli --test info`, and `cargo nextest run --workspace` (122 passing tests). Committed as `07fafa9` with subject `refactor(windows): consolidate steam registry sources`.
* **Restore hint contract tightening**: Unified restore-hint formatting across human CLI outputs, locked Steam restore-hint JSON contracts for `scan`, `clean --dry-run`, and `history`, and preserved `restore_hint` through core plan serialization. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-core --test model_contract`, `cargo nextest run -p rebecca-cli --test cli_scan -p rebecca-cli --test cli_clean -p rebecca-cli --test cli_history`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (127 passing tests). Committed as `c7afc04` with subject `test(core): preserve restore hints in plan serialization`, after earlier CLI contract commits `06eaf79`, `d77d25e`, `7e3ca4d`, and `79b4baf`.
* **Steam plan alignment**: Updated the Steam expansion plan and current-state memory to reflect that the implemented slice now includes catalog growth, discovery hardening, and restore-hint contract alignment, so later sessions do not continue from stale "next active plan" wording.
* **Category selection validation**: Added explicit validation for `scan` and `clean` category filters so invalid categories now fail with `invalid category` instead of silently producing an empty filtered set. Added core and CLI regressions, re-verified with `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (133 passing tests). Committed as `3f8455e` with subject `fix(core): validate category selections explicitly`.
* **Selection validation refactor**: Moved category and rule-id selection validation onto `RuleSelection` itself so `scan` and planner construction share one semantic validation entry point. Added core regressions for unknown categories and rule ids through the new method, and re-verified with `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (135 passing tests). Committed as `cca8b14` with subject `refactor(core): move selection validation onto rule selection`.
* **Selection matching simplification**: Extracted shared case-insensitive matching inside `RuleSelection` so category and rule-id checks use one internal helper, added a validation-layer case-insensitivity regression, and re-verified with `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (136 passing tests). Committed as `85cbbae` with subject `refactor(core): simplify rule selection matching`.
* **Steam install shader cache**: Added a conservative `windows.steam-install-shader-cache` rule for Steam's install-root `appcache\\shadercache`, plus planner, CLI, README, and rule-authoring coverage. Re-verified with `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (138 passing tests). Committed as `2ef0814` with subject `feat(core): add steam install shader cache rule`.
* **Steam install logs**: Added a conservative `windows.steam-install-logs` rule for Steam's install-root `logs` directory, based on Valve's Steamworks debugging docs, plus planner, CLI, README, and rule-authoring coverage. Re-verified with `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (140 passing tests).
* **Steam install depot cache**: Added a conservative `windows.steam-install-depot-cache` rule for Steam's install-root `depotcache` directory, based on Valve's Steamworks documentation, plus planner, CLI, README, and rule-authoring coverage. Re-verified with `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace`.
* **Steam target resolution simplification**: Collapsed the Steam install/library target resolution branches in `rebecca-core/src/discovery.rs` into a shared helper, keeping the safe-relative validation and skip behavior intact. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-core --test discovery -p rebecca-core --test planner`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (142 passing tests).
* **Builtin catalog loader simplification**: Replaced the long `rebecca-rules` builtin TOML include list with a small macro-backed declarative list, keeping rule order and validation unchanged. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-rules`, and `cargo nextest run --workspace` (142 passing tests). This followed the earlier `aa038bd` test-helper refactor in the same iteration.
* **Steam registry source table simplification**: Replaced the `rebecca-windows` Steam registry source function with a static ordered constant table, keeping the discovery precedence and tests unchanged. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-windows --test recycle_bin`, `cargo nextest run -p rebecca-core --test discovery -p rebecca-core --test planner`, and `cargo nextest run --workspace` (142 passing tests).
* **Steam libraryfolders candidate tightening**: Tightened the Steam installation libraryfolders reader by making the candidate paths declarative and isolating the file-read helper, while preserving the existing fallback and error behavior. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-cli --test info -p rebecca-cli --test cli_output -p rebecca-cli --test cli_clean`, `cargo nextest run -p rebecca-core --test discovery -p rebecca-core --test planner`, and `cargo nextest run --workspace` (142 passing tests).
* **Steam wrapper removal**: Removed the redundant `rebecca-windows` Steam installation wrapper and routed Windows discovery directly through `SteamInstallation::from_install_path_best_effort`, then updated the Windows tests to use the core helper directly. Re-verified with `cargo fmt --all` and `cargo nextest run -p rebecca-windows --test recycle_bin -p rebecca-core --test discovery -p rebecca-core --test planner -p rebecca-cli --test info -p rebecca-cli --test cli_output -p rebecca-cli --test cli_clean` (80 passing tests in the targeted run).
* **Steam relative-target helper simplification**: Folded the shared Steam relative-target resolution path inside `rebecca-core::discovery` into a small scope enum plus direct iterator branches, keeping install-only and install-plus-library behavior unchanged. Re-verified with `cargo fmt --all` and `cargo nextest run -p rebecca-core --test discovery -p rebecca-core --test planner -p rebecca-cli --test info -p rebecca-cli --test cli_clean` (72 passing tests in the targeted run).
* **Steam rule baseline alignment**: Aligned the Steam built-in rule coverage baseline by adding `windows.steam-install-logs` to the catalog coverage test, keeping the recorded rule list in sync with the shipped catalog. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-rules -p rebecca-core --test discovery -p rebecca-core --test planner -p rebecca-cli --test cli_scan -p rebecca-cli --test scan`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (142 passing tests).
* **Steam test refactor and docs alignment**: Collapsed the Steam planner and CLI regression coverage into table-driven tests, keeping the same coverage while reducing the repeated case-by-case assertions. Refreshed the README, `docs/rule-authoring.md`, and engineering memory to keep the Steam catalog boundary wording explicit. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-core --test planner -p rebecca-cli --test cli_clean -p rebecca-cli --test cli_scan`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (132 passing tests).
* **Steam avatar cache**: Added a conservative `windows.steam-install-avatar-cache` rule for Steam's install-root `config\\avatarcache`, plus planner, CLI, README, and rule-authoring coverage. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-rules -p rebecca-core --test planner -p rebecca-cli --test cli_clean -p rebecca-cli --test cli_scan -p rebecca-cli --test scan`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (132 passing tests).
* **Steam test deepening**: Moved Steam rule cases and builtin rule expectations into repo-level shared test helpers so the CLI and core regression suites consume one Steam fixture table instead of repeating the same arrays. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-core --test planner -p rebecca-cli --test scan -p rebecca-cli --test cli_scan -p rebecca-cli --test cli_clean`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (132 passing tests).
* **Steam install stats cache**: Added a conservative `windows.steam-install-stats-cache` rule for Steam's install-root `appcache\\stats`, plus catalog, shared Steam test data, README, and rule-authoring coverage. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-rules -p rebecca-core --test planner -p rebecca-cli --test scan -p rebecca-cli --test cli_scan -p rebecca-cli --test cli_clean`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (132 passing tests).
* **Steam install appinfo cache**: Added a conservative `windows.steam-install-appinfo-cache` rule for Steam's install-root `appcache\\appinfo.vdf`, and extended the shared Steam test helper to support file targets so the new rule can be covered alongside the existing directory rules. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-rules -p rebecca-core --test planner -p rebecca-cli --test scan -p rebecca-cli --test cli_scan -p rebecca-cli --test cli_clean`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (132 passing tests).
* **Steam helper borrowing simplification**: Switched the shared Steam fixture helper and its CLI/core callers to borrowed cases instead of copied values, which keeps the table-driven Steam tests simpler without changing their assertions or fixture layout.
* **Steam install localization and package info caches**: Added conservative `windows.steam-install-localization-cache` and `windows.steam-install-packageinfo-cache` rules for the Steam install-root `appcache\\localization.vdf` and `appcache\\packageinfo.vdf` files, then extended the shared Steam fixture table, builtin catalog loader, README, and rule-authoring guidance to keep the new file rules visible and auditable. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-rules -p rebecca-core --test planner -p rebecca-cli --test scan -p rebecca-cli --test cli_scan -p rebecca-cli --test cli_clean`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (132 passing tests).
* **Configuration/local-state contract doc**: Added `docs/configuration.md` as the canonical contract for config schema v1, path precedence, local-state lifecycle ownership, cache purge boundaries, privacy, and migration rules. Linked it from README and ADR 0008, tightened rule-authoring guidance so built-in rules do not target Rebecca-owned storage, and re-verified with `cargo nextest run -p rebecca-core config`, `cargo nextest run -p rebecca-cli --test cli_output`, and `cargo nextest run -p rebecca-cli --test cli_cache`.
* **Protected-result history/output preservation**: Preserved protected and blocked cleanup issue details across plan JSON, history JSONL, `history --format json`, and human `history` output. Added core model/history round-trip coverage, CLI history JSON/human regressions, and clean human blocked-target output coverage; refreshed the safety audit so U7 guardrailed rule expansion is now the next Mole-parity gap. Re-verified with `cargo fmt --all -- --check`, `cargo nextest run -p rebecca-core --test model_contract`, `cargo nextest run -p rebecca-core --test history`, `cargo nextest run -p rebecca-cli --test cli_history`, `cargo nextest run -p rebecca-cli --test cli_clean`, `cargo nextest run --workspace` (239 passing tests), and `cargo clippy --workspace --all-targets -- -D warnings`.
* **Slack guardrailed rule expansion**: Added a conservative `windows.slack-cache` rule for Slack's Roaming `Cache`, `Code Cache`, and `GPUCache` directories only. Refactored the Electron cache allowlist in `ProtectionPolicy`, added safety/planner near-miss coverage for Slack `Local Storage`, `IndexedDB`, and `Service Worker`, updated scan and clean CLI contracts plus README/rule-authoring/safety-audit docs, and re-verified with `cargo fmt --all -- --check`, `cargo nextest run -p rebecca-core --test safety_policy`, `cargo nextest run -p rebecca-core --test planner`, `cargo nextest run -p rebecca-rules`, `cargo nextest run -p rebecca-cli --test cli_scan`, `cargo nextest run -p rebecca-cli --test scan`, `cargo nextest run -p rebecca-cli --test cli_clean`, `cargo nextest run --workspace` (242 passing tests), and `cargo clippy --workspace --all-targets -- -D warnings`.
* **Mole-parity completion review**: Added root `SECURITY.md`, linked it from README and the safety audit, recorded the U8 requirement-by-requirement evidence in `docs/knowledge/engineering/verification/2026-06-24-mole-parity-completion-review.md`, and marked the engineering memory current state complete for the Windows-first cleanup safety governance goal. Distribution-layer release attestations and installer integrity are now explicitly future work outside the completed cleanup-system objective.
* **Release integrity baseline**: Added `docs/plans/2026-06-24-019-feat-release-integrity-and-distribution-plan.md`, Windows CI and tag-triggered release workflows, PowerShell release packaging and checksum scripts, and `docs/release.md` with checksum plus GitHub attestation verification guidance. README, `SECURITY.md`, and `docs/security-audit.md` now treat release/install trust as a first-class security surface. Re-verified with local release smoke packaging, checksum comparison, `cargo fmt --all -- --check`, `cargo nextest run --workspace` (242 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.
* **Windows install-time verification**: Added `docs/plans/2026-06-24-020-feat-windows-install-verification-plan.md` and `scripts/install.ps1`. The installer downloads or consumes a local release ZIP plus `SHA256SUMS`, always verifies checksum before extraction, supports optional GitHub CLI attestation verification, and fails closed with `-RequireAttestation`. Release packaging now includes the install script and `repository=` metadata in `VERSION.txt`. README, `docs/release.md`, `SECURITY.md`, and `docs/security-audit.md` now document install/update, PATH, checksum-only, and attestation-required modes. Re-verified with release-smoke packaging, install-smoke success, bad-checksum rejection before install, required-attestation rejection before install, `cargo fmt --all -- --check`, `cargo nextest run --workspace` (242 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.

## 2026-06-26

* **sccache compiler-cache coverage**: Added a Windows-first `windows.sccache-cache` rule for sccache's local disk cache, covering the default `%LOCALAPPDATA%\\Mozilla\\sccache` root and user-configured `SCCACHE_DIR`. The protection policy now explicitly allowlists the default local cache root, and the rule-authoring / safety-audit / README docs now call out sccache as a separate compiler-cache family rather than folding it into Cargo. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-rules -p rebecca-core --test planner -p rebecca-core --test safety_policy -p rebecca-cli --test scan -p rebecca-cli --test cli_scan`, `cargo nextest run --workspace` (310 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`. Committed as `7f0e737` (`feat(rules): add sccache cache coverage`).
* **Project-artifact purge UX and catalog**: Improved `purge` human output so project-artifact plans group by project path, label artifact types separately, and highlight recently modified skips. Added a scan-free `purge --list-artifacts` selector catalog with human and JSON output, exposed the complete project-artifact definition iterator from `rebecca-core`, and refreshed the CLI docs plus project-artifact config contract. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-cli --test cli_purge`, `cargo nextest run -p rebecca-core --test project_artifacts`, `cargo nextest run -p rebecca-core --test planner`, `cargo nextest run -p rebecca-rules`, `cargo nextest run --workspace` (302 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`. Committed as `f0bdeb7` (`feat(purge): improve project artifact output`) and `43a13c6` (`feat(purge): list project artifact selectors`).
* **Brave browser cache coverage**: Added a conservative `windows.brave-cache` rule for Brave's Chromium-family cache directories, covering `Default` and bounded `Profile *` `Cache`, `Code Cache`, and `GPUCache` paths only. Added planner and shared test coverage, updated the builtin rule list, refreshed README wording, and re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-rules`, `cargo nextest run -p rebecca-core --test planner`, `cargo nextest run -p rebecca-core --test project_artifacts`, `cargo nextest run -p rebecca-cli --test cli_scan`, `cargo nextest run --workspace` (302 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`. Committed as `e67b4e7` with subject `feat(rules): add brave browser cache coverage`.
* **Engineering memory refresh**: Synced the safety audit and current-state memory with the latest cleanup UX and Brave catalog work so future sessions see the current `purge --list-artifacts` surface, the Brave cache batch, and the next cleanup-slice direction without reading the full chat history. Re-checked the updated memory files with `git diff --check`.
* **Node package-manager cache coverage**: Corrected `windows.npm-cache` to include the Windows Local AppData default while keeping the older Roaming fallback, then added conservative moderate rules for pnpm store, Yarn cache, Bun package cache, and Corepack cache. Updated the shared builtin rule expectations, README, and safety-audit maintenance allowlist; added planner coverage that proves the new rules target rebuildable caches rather than Bun binaries or Yarn global data; and refreshed CLI clean/scan regressions for the expanded npm target set. Re-verified with `cargo fmt --all -- --check`, `cargo nextest run -p rebecca-rules`, `cargo nextest run -p rebecca-core --test planner`, `cargo nextest run -p rebecca-cli --test cli_clean`, `cargo nextest run -p rebecca-cli --test cli_scan`, `cargo nextest run -p rebecca-cli --test scan`, `cargo nextest run -p rebecca-cli --test output`, `cargo nextest run --workspace` (303 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, `git diff --check`, and engineering wiki memory validation.
* **NuGet cache coverage**: Added a Windows-first `windows.nuget-cache` rule covering `%USERPROFILE%\\.nuget\\packages`, `%LOCALAPPDATA%\\NuGet\\v3-cache`, `%LOCALAPPDATA%\\NuGet\\plugins-cache`, and `%LOCALAPPDATA%\\NuGet\\Cache` as moderate rebuildable package caches. Updated the protection-policy allowlist, builtin rule expectations, README, and safety audit; added planner coverage proving NuGet config files are not targeted. Re-verified with `cargo fmt --all -- --check`, `cargo nextest run -p rebecca-rules`, `cargo nextest run -p rebecca-core --test planner`, `cargo nextest run -p rebecca-cli --test cli_scan`, `cargo nextest run -p rebecca-cli --test scan`, `cargo nextest run --workspace` (304 passing tests), and `cargo clippy --workspace --all-targets -- -D warnings`.
* **JVM cache coverage**: Added Windows-first `windows.gradle-cache` and `windows.maven-cache` rules for rebuildable Gradle caches, Gradle notifications, and Maven local repository artifacts. Updated the protection-policy allowlist, builtin rule expectations, README, and safety audit; added planner and safety-policy coverage proving Gradle wrapper distributions and Maven settings files are not targeted. Re-verified with `cargo fmt --all -- --check`, `cargo nextest run -p rebecca-rules`, `cargo nextest run -p rebecca-core --test planner`, `cargo nextest run -p rebecca-core --test safety_policy`, `cargo nextest run -p rebecca-cli --test cli_scan`, `cargo nextest run -p rebecca-cli --test scan`, `cargo nextest run --workspace` (305 passing tests), and `cargo clippy --workspace --all-targets -- -D warnings`.
* **Python package-manager cache coverage**: Added Windows-first `windows.uv-cache` and `windows.poetry-cache` rules for uv's Local AppData cache and Poetry's package metadata/artifact cache subdirectories. The rule intentionally avoids Poetry virtualenvs, uv tools, Python installations, and project-local tool caches; the protection policy now treats Poetry virtualenvs as durable application state. Updated builtin rule expectations, README, rule-authoring guidance, and the safety audit; added planner and safety-policy coverage for the new boundary. Re-verified with `cargo fmt --all -- --check`, `cargo nextest run -p rebecca-rules`, `cargo nextest run -p rebecca-core --test planner`, `cargo nextest run -p rebecca-core --test safety_policy`, `cargo nextest run -p rebecca-cli --test cli_scan`, `cargo nextest run -p rebecca-cli --test scan`, `cargo nextest run --workspace` (306 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.
* **Go cache coverage**: Added Windows-first `windows.go-build-cache` and `windows.go-module-cache` rules for `%LOCALAPPDATA%\\go-build` and the default GOPATH module cache `%USERPROFILE%\\go\\pkg\\mod`. The rule intentionally avoids GOPATH `bin`, `src`, and broad `%USERPROFILE%\\go\\pkg` compiled package output. Updated the protection-policy allowlist, builtin rule expectations, README, rule-authoring guidance, and the safety audit; added planner and safety-policy coverage for the new boundary. Re-verified with `cargo fmt --all -- --check`, `cargo nextest run -p rebecca-rules`, `cargo nextest run -p rebecca-core --test planner`, `cargo nextest run -p rebecca-core --test safety_policy`, `cargo nextest run -p rebecca-cli --test cli_scan`, `cargo nextest run --workspace` (307 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.
* **Rustup and Conda cache coverage**: Added Windows-first `windows.rustup-cache` and `windows.conda-cache` rules for rebuildable Rustup downloads/tmp data and Conda package caches, respectively. The protection policy now keeps Rustup toolchains/settings/overrides and Conda environments out of the cleanup surface, and the safety docs / rule-authoring guidance spell out both cache-vs-state boundaries. Re-verified with `cargo fmt --all -- --check`, `cargo nextest run -p rebecca-rules -p rebecca-core --test planner -p rebecca-core --test safety_policy`, `cargo nextest run --workspace` (309 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`. Committed as `4cf1521` (`feat(rules): add rustup cache coverage`) and `451fcbe` (`feat(rules): add conda cache coverage`).
* **Rule source index**: Added `docs/knowledge/engineering/conventions/rule-sources.md` as the canonical external-source index for rule authoring. It records Mole, BleachBit, Winapp2.ini, windows-cleaner-cli, and Bulk Crap Uninstaller as reference inputs with license and usage notes, and the rule authoring / safety audit / current-state docs now point rule families at that index so provenance notes can preserve upstream project, file path, license, and revision details. Re-verified with `cargo fmt --all -- --check` and `git diff --check`.
* **ccache compiler-cache coverage**: Added a Windows-first `windows.ccache-cache` rule for ccache's two-level local cache buckets and `tmp` directory under `CCACHE_DIR`, `%USERPROFILE%\\.ccache`, `%LOCALAPPDATA%\\ccache`, and the legacy `%APPDATA%\\ccache` root. The rule intentionally avoids `ccache.conf`, `stats`, and `CACHEDIR.TAG`; the protection policy now keeps those durable/configuration leaves blocked while allowlisting only rebuildable cache bucket shapes. Updated the shared builtin rule expectations, README, rule-authoring guidance, and safety audit; added planner and safety-policy coverage for configured and default ccache roots. Re-verified with `cargo fmt --all`, `cargo nextest run --workspace` (311 passing tests), and `cargo clippy --workspace --all-targets -- -D warnings`. Committed as `fb3ea2b` (`feat(rules): add ccache cache coverage`).
* **Windows maintenance-cache slice**: Added Windows-first rules for `%WINDIR%\\Temp`, `%WINDIR%\\Prefetch`, `%WINDIR%\\SoftwareDistribution\\Download`, and Windows Media Player cache paths. The protection policy now allowlists those maintenance caches, the builtin rule list now includes `windows.system-temp`, `windows.prefetch`, `windows.update-download-cache`, and `windows.media-player-cache`, and the rule-authoring / safety-audit docs now treat `windows-cleaner-cli` as a reference for Windows maintenance-cache families. Re-verified in subsequent full-workspace gates through the Android cache batch, including `cargo nextest run --workspace` (313 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`. Committed as `254ff36` (`feat(rules): add windows maintenance cache rules`).
* **Reference-source split clarified**: `windows-cleaner-cli` is now the preferred batch reference for Windows maintenance caches, `null-e` is the preferred batch reference for developer caches, `BleachBit` remains the behavior reference for Windows application/system cleaners, and `Bulk Crap Uninstaller` remains the uninstall-leftover reference. The source index now records revision hashes so future rule batches can preserve provenance without re-discovery, and later ML/AI plus Android rule batches extended it with primary-source checks.
* **ML model cache coverage**: Added Windows-first `windows.huggingface-cache` and `windows.pytorch-cache` rules for Hugging Face cache roots and PyTorch hub cache roots. The rules keep token/account state and broad model roots out of the cleanup surface, and the protection policy plus planner tests cover the cache-vs-durable-state boundary. Re-verified with `cargo fmt --all`, targeted nextest, `cargo nextest run --workspace` (312 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`. Committed as `bf0bf26` (`feat(rules): add ml model cache coverage`).
* **Android development cache coverage**: Added Windows-first `windows.android-cache` for `.android` cache leaves and Android Studio cache directories only. AVDs, SDK packages, system images, licenses, adb/debug keys, and IDE settings/plugins remain protected durable state. Re-verified with `cargo fmt --all`, targeted nextest, CLI scan tests, `cargo nextest run --workspace` (313 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`. Committed as `c84501f` (`feat(rules): add android cache coverage`).
* **Electron app cache coverage**: Added Windows-first `windows.postman-cache`, `windows.notion-cache`, and `windows.figma-cache` rules for Roaming Electron cache leaves only. The shared protection policy allowlists only `Cache`, `Code Cache`, and `GPUCache` for those roots while keeping `Local Storage`, `IndexedDB`, `Service Worker`, `Network`, settings, workspace data, and session data outside the cleanup surface. Re-verified with `cargo fmt --all`, targeted nextest, memory validation, `cargo nextest run --workspace` (314 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.
* **Release SBOM foundation**: Added `scripts/release/write-sbom.ps1` to generate SPDX 2.3 tag-value SBOMs from `cargo metadata --locked --filter-platform`, include the release ZIP SHA-256, and publish the SBOM beside the ZIP and `SHA256SUMS` in the GitHub release workflow. Checksums and attestations now cover the SBOM as a first-class release asset. Re-verified with release SBOM smoke (`build-release.ps1 -SkipBuild`, `write-sbom.ps1`, `write-checksums.ps1`), `cargo fmt --all`, `cargo nextest run --workspace` (314 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.
