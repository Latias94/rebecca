# Engineering Memory Update Log

## 2026-06-23
* **Initialization**: Created engineering wiki memory bundle.
* **Documentation baseline**: Collected reference projects and started Windows-first ADR drafting for the Rust CLI cleaner.
* **ADR baseline**: Added platform strategy and core runtime architecture ADRs; validated the engineering memory bundle.
* **Boundary ADRs**: Added workspace, Windows privilege/registry, and scan engine ADRs; updated memory bundle state.
* **Safety and state ADRs**: Added deletion/recovery, rule provenance, and configuration/local-state ADRs.
* **Workspace bootstrap**: Initialized the `rebecca` Rust workspace and verified it with `cargo fmt`, `cargo check --workspace`, and `cargo nextest run --workspace`.
* **State layout refinement**: Split local state and cache into distinct `state/` and `cache/` subdirectories under `%LOCALAPPDATA%\\Rebecca`.
* **MVP implementation plan**: Added `docs/plans/2026-06-23-001-feat-windows-cleanup-mvp-plan.md` to define the first Windows cleanup loop: owned rules, path expansion, safety policy, parallel scan, Recycle Bin execution, history, CLI output, and tests.
* **Windows cleanup MVP implementation**: Implemented the plan-first cleanup loop with typed rule targets, environment path expansion, safety policy, scanner, planner, execution backend contract, Windows Recycle Bin backend, history JSONL store, CLI commands, README, and 31 passing tests.
* **MVP commit**: Committed the Windows cleanup MVP with subject `feat: build windows cleanup mvp`; post-commit verification passed with `cargo fmt --all -- --check`, `cargo check --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace`.
* **Overlapping template dedupe fix**: Fixed planner deduplication so overlapping templates such as `%TEMP%` and `%LOCALAPPDATA%\\Temp` do not double-count the same path. Added regression tests and re-verified with 33 passing tests plus real dry-run smoke runs for `system`, `browser`, and `development --allow-moderate`.
* **TOML rule catalog**: Moved the three built-in Windows rules from Rust constructors into TOML files under `crates/rebecca-rules/rules/windows/`; added strict TOML schema parsing, duplicate target spec validation, documentation updates, and 37 passing workspace tests.
* **First rule expansion batch**: Added `docs/rule-authoring.md` and expanded the TOML-backed catalog from 3 to 8 rules with Chrome cache, DirectX shader cache, pip cache, VS Code cache, and Windows Error Reporting data. Re-verified with 38 passing workspace tests, `cargo run -p rebecca-cli -- scan`, and a dry-run smoke over the five new rules.
* **Glob target discovery**: Added `glob-template` target specs, bounded wildcard path discovery, planner expansion for multi-path targets, Firefox profile cache and Windows thumbnail/icon cache rules, and rule authoring guidance. Re-verified with 43 passing workspace tests, `cargo run -q -p rebecca-cli -- scan`, and dry-run smoke over the new glob rules.
* **Rule selection UX**: Added reusable `RuleSelection` semantics, `scan --category` and `scan --rule` filters, grouped human scan output, README examples, and CLI regression tests. Re-verified with 46 passing workspace tests and smoke runs for browser-category and thumbnail-rule scans.
* **Chromium profile rules**: Expanded Chrome and Edge cache rules to cover explicit `Default` cache paths and bounded `Profile *` Cache, Code Cache, and GPUCache discovery. Added planner regression coverage and re-verified with 47 passing workspace tests plus Chrome/Edge dry-run smoke.
* **Dry-run output readability**: Improved human cleanup plan output with byte formatting, a largest-estimated-targets section, and status-grouped target details while preserving JSON output. Added CLI regression coverage and re-verified with 48 passing workspace tests plus Chrome/Edge human dry-run smoke.
* **Target-level scan progress**: Added planner progress events, wired `clean` to an `indicatif` spinner for human output, and added `--no-progress` for quiet logs while keeping JSON output silent. Added core and CLI regression coverage and re-verified with 50 passing workspace tests.
* **JetBrains cache batch**: Added a `windows.jetbrains-cache` rule for bounded JetBrains `caches` directories under `%LOCALAPPDATA%\\JetBrains\\<product><version>`, plus scan and planner coverage. Re-verified with 51 passing workspace tests and JetBrains scan/clean smoke.
* **Cargo cache batch**: Added a `windows.cargo-cache` rule for `%CARGO_HOME%` and default `%USERPROFILE%\\.cargo` cache subdirectories, including `registry/cache`, `registry/index`, `registry/src`, `git/db`, and `git/checkouts`. Added planner coverage for default and custom `CARGO_HOME` layouts plus CLI rule listing coverage, and updated rule-authoring guidance. Re-verified with `cargo fmt --all --check`, `cargo nextest run -p rebecca-rules`, `cargo nextest run -p rebecca-core --test planner`, and `cargo nextest run -p rebecca-cli --test cli_scan`.
* **Discord cache batch**: Added a `windows.discord-cache` rule for Discord Roaming cache directories under `%APPDATA%\\discord`, `%APPDATA%\\discordptb`, and `%APPDATA%\\discordcanary`, covering `Cache`, `Code Cache`, and `GPUCache` only. Added planner coverage for stable and Canary layouts plus CLI rule listing coverage, updated rule-authoring guidance for Electron caches, and re-verified with `cargo fmt --all --check` and `cargo nextest run --workspace` (54 passing tests).
* **File-level progress and cancellation**: Added file-level scan progress events to the core scanner, cancellation tokens for long scans, planner propagation of per-file progress, Ctrl+C cancellation wiring in the CLI, and README guidance for the new behavior. Added core planner/scan regression coverage and re-verified with `cargo fmt --all --check` and `cargo nextest run --workspace` (57 passing tests).
* **Scan performance baseline**: Added structured scan reports, scan failure classification, and a Criterion benchmark baseline for `rebecca-core` scanning 1,024 files with and without file-level progress callbacks. Added core regression coverage for structured scan errors and report counts, and re-verified with `cargo fmt --all --check`, `cargo nextest run --workspace` (60 passing tests), `cargo check -p rebecca-core --benches`, and a shortened benchmark run. Latest benchmark range: `scan_report_1024_files` about 18.6-20.8 ms; `scan_report_with_file_progress_1024_files` about 14.3-14.8 ms.
* **Steam cache batch**: Added a conservative `windows.steam-cache` rule for Steam's client web cache under `%LOCALAPPDATA%\\Steam\\htmlcache\\Default`, covering `Cache`, `Code Cache`, and `GPUCache` only. Added planner coverage to prove the rule ignores Steam `userdata`, `steamapps`, `Local Storage`, and `IndexedDB`, plus CLI rule listing coverage, updated rule-authoring guidance, and re-verified with `cargo fmt --all --check` and `cargo nextest run --workspace` (61 passing tests).
* **Steam discovery base**: Added Steam installation/library discovery plumbing in `rebecca-core` and `rebecca-windows`, including `libraryfolders.vdf` parsing for current and legacy formats, safe relative Steam target kinds, planner support for application discovery injection, and a `doctor steam` CLI command. Added discovery/planner/CLI/recycle-bin regression coverage and re-verified with `cargo fmt --all --check` and `cargo nextest run --workspace` (72 passing tests).
* **Steam library expansion**: Added conservative Steam library download-staging and temp-cache rules for `steamapps\downloading` and `steamapps\temp`, plus planner and CLI regression coverage, rule catalog registration, and README / rule-authoring updates. Re-verified with `cargo clippy --workspace --all-targets --all-features -- -D warnings` and `cargo nextest run --workspace` (83 passing tests).
* **Steam install library cache**: Added a conservative `windows.steam-install-library-cache` rule for Steam's install-root `appcache\\librarycache`, plus planner skip coverage for missing Steam discovery, CLI rule listing coverage, and README / rule-authoring updates. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-rules`, `cargo nextest run -p rebecca-core --test planner`, and `cargo nextest run -p rebecca-cli --test cli_scan`.
* **Steam CLI regression coverage**: Added deterministic CLI regression coverage for Steam discovery by wiring a debug-only discovery override into the `doctor steam` and `clean --dry-run` test paths, then verified the full Steam slice with `cargo fmt --all`, `cargo nextest run -p rebecca-cli --test cli_clean`, `cargo nextest run -p rebecca-cli --test cli_output`, `cargo nextest run -p rebecca-core --test planner`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and `cargo nextest run --workspace` (88 passing tests).
* **Moderate rule allow coverage**: Added a CLI regression that proves `windows.npm-cache` stays skipped without `--allow-moderate` and becomes allowed when the flag is present, then re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-cli --test cli_clean`, `cargo nextest run -p rebecca-core --test planner`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and `cargo nextest run --workspace` (89 passing tests).
* **Safety contract coverage**: Added planner and CLI regressions for `allow_risky` using temporary risky rules so the parser/planner contract is explicit even without a built-in risky catalog entry.
* **README safety example**: Added opt-in `--allow-moderate` and `--allow-risky` usage examples to README so the documented CLI mirrors the tested contract.

## 2026-06-24
* **Rule catalog governance hardening**: Tightened catalog validation so invalid rules with empty category, name, or target paths fail centrally, and added built-in-only guardrails requiring Windows platform, `windows.` id prefixes, non-empty restore hints, owned source, and `project-owned` provenance. Updated rule-authoring guidance and re-verified with `cargo nextest run -p rebecca-rules`, `cargo nextest run -p rebecca-core --test model_contract`, `cargo nextest run --workspace` (223 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.
* **Clean storage-boundary enforcement**: Enforced the configuration/local-state contract in `rebecca clean` by passing Rebecca-owned storage entries into the planner and blocking targets that overlap config, state, history, or cache paths. Extracted shared path-overlap logic for planner and cache purge boundary checks, added core and CLI regressions for default/custom app paths, refreshed the configuration contract, and re-verified with `cargo nextest run -p rebecca-core --test planner`, `cargo nextest run -p rebecca-core cache`, `cargo nextest run -p rebecca-cli --test cli_clean`, `cargo nextest run --workspace` (215 passing tests), `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check`.
* **Plan closure**: Marked the Windows cleanup MVP plan complete and aligned the current-state memory with the finished MVP.
* **Steam expansion plan**: Drafted `docs/plans/2026-06-24-002-feat-steam-cleanup-expansion-plan.md` to add conservative Steam install-root and library-root cleanup coverage.
* **Steam discovery path normalization**: Normalized Steam install and library path keys so trailing separators no longer produce duplicate discoveries, added regression coverage for install-root and libraryfolders parsing, and verified the workspace with `cargo fmt --all`, `cargo nextest run -p rebecca-core --test discovery`, `cargo nextest run -p rebecca-core --test planner`, and `cargo nextest run --workspace` (106 passing tests). Committed as `5948f3c` with subject `fix(core): normalize steam discovery path keys`.
* **Steam discovery path validation**: Tightened Steam discovery so relative or current-directory library entries are ignored, added regression coverage for the parser and relative Steam templates, and re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-core --test discovery`, `cargo nextest run -p rebecca-core --test planner`, and `cargo nextest run --workspace` (108 passing tests). Committed as `e466fab` with subject `fix(core): tighten steam discovery path validation`.
* **Steam discovery fallback hardening**: Centralized Steam discovery loading so unreadable `libraryfolders.vdf` data now falls back to the install root instead of dropping the discovery result, added regression coverage in `rebecca-windows` and `rebecca-cli`, and re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-windows --test recycle_bin`, `cargo nextest run -p rebecca-core --test discovery`, `cargo nextest run -p rebecca-core --test planner`, `cargo nextest run -p rebecca-cli --test info`, and `cargo nextest run --workspace` (111 passing tests). Committed as `05d3fa1` with subject `fix(steam): fall back when discovery data is unreadable`.
* **Steam libraryfolders merge**: Updated Steam installation discovery to read `libraryfolders.vdf` from both `config` and `steamapps`, merge the discovered library roots, and keep the install root usable when one file is missing. Added regression coverage for dual-source discovery, missing-but-readable cases, and CLI output. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-core --test discovery`, `cargo nextest run -p rebecca-core --test planner`, `cargo nextest run -p rebecca-cli --test info`, and `cargo nextest run --workspace` (114 passing tests). Committed as `a150322` with subject `fix(steam): merge Steam libraryfolders locations`.
* **Steam registry fallback hardening**: Added Windows registry fallback discovery for Steam install paths, covering `HKCU\Software\Valve\Steam\SteamPath`, `HKLM\SOFTWARE\Valve\Steam\InstallPath`, `HKLM\SOFTWARE\WOW6432Node\Valve\Steam\InstallPath`, and `HKCR\steam\Shell\Open\Command`, then split command parsing into a pure helper with unit coverage. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-windows --test recycle_bin`, `cargo nextest run -p rebecca-core --test discovery`, `cargo nextest run -p rebecca-cli --test info`, and `cargo nextest run --workspace` (117 passing tests). Committed as `71e2988` with subject `fix(windows): add steam registry fallback`.
* **Steam registry command parsing refinement**: Deepened the Steam registry fallback seam by extracting command-to-install-path parsing into a pure helper, tightening the unquoted-command path detection to avoid `.exe` misfires, and adding targeted unit coverage for quoted and unquoted Steam command strings. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-windows`, `cargo nextest run -p rebecca-core --test discovery`, `cargo nextest run -p rebecca-cli --test info`, and `cargo nextest run --workspace` (120 passing tests). Committed as `839b703` with subject `refactor(windows): deepen steam command parsing`.
* **Steam HKCU SteamExe fallback**: Added a direct `HKCU\Software\Valve\Steam\SteamExe` fallback before the broader legacy registry search, refactored registry string loading into a reusable helper, and kept the command-path seam covered with focused tests. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-windows`, `cargo nextest run -p rebecca-core --test discovery`, `cargo nextest run -p rebecca-cli --test info`, and `cargo nextest run --workspace` (121 passing tests). Committed as `f1553fb9` with subject `fix(windows): add steamexe registry fallback`.
* **Steam registry source table**: Consolidated Steam discovery into a single ordered registry source table covering `SteamPath`, `SteamExe`, `InstallPath`, and `Shell\Open\Command`, keeping the lookup logic deeper and the fallback order explicit. Added a regression that pins the source order and re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-windows`, `cargo nextest run -p rebecca-core --test discovery`, `cargo nextest run -p rebecca-cli --test info`, and `cargo nextest run --workspace` (122 passing tests). Committed as `07fafa9` with subject `refactor(windows): consolidate steam registry sources`.
* **Restore hint contract tightening**: Unified restore-hint formatting across human CLI outputs, locked Steam restore-hint JSON contracts for `scan`, `clean --dry-run`, and `history`, and preserved `restore_hint` through core plan serialization. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-core --test model_contract`, `cargo nextest run -p rebecca-cli --test cli_scan -p rebecca-cli --test cli_clean -p rebecca-cli --test cli_history`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (127 passing tests). Committed as `c7afc04` with subject `test(core): preserve restore hints in plan serialization`, after earlier CLI contract commits `06eaf79`, `d77d25e`, `7e3ca4d`, and `79b4baf`.
* **Steam plan alignment**: Updated the Steam expansion plan and current-state memory to reflect that the implemented slice now includes catalog growth, discovery hardening, and restore-hint contract alignment, so later sessions do not continue from stale "next active plan" wording.
* **Category selection validation**: Added explicit validation for `scan` and `clean` category filters so invalid categories now fail with `invalid category` instead of silently producing an empty filtered set. Added core and CLI regressions, re-verified with `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (133 passing tests). Committed as `3f8455e` with subject `fix(core): validate category selections explicitly`.
* **Selection validation refactor**: Moved category and rule-id selection validation onto `RuleSelection` itself so `scan` and planner construction share one semantic validation entry point. Added core regressions for unknown categories and rule ids through the new method, and re-verified with `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (135 passing tests). Committed as `cca8b14` with subject `refactor(core): move selection validation onto rule selection`.
* **Selection matching simplification**: Extracted shared case-insensitive matching inside `RuleSelection` so category and rule-id checks use one internal helper, added a validation-layer case-insensitivity regression, and re-verified with `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (136 passing tests). Committed as `85cbbae` with subject `refactor(core): simplify rule selection matching`.
* **Steam install shader cache**: Added a conservative `windows.steam-install-shader-cache` rule for Steam's install-root `appcache\\shadercache`, plus planner, CLI, README, and rule-authoring coverage. Re-verified with `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (138 passing tests). Committed as `2ef0814` with subject `feat(core): add steam install shader cache rule`.
* **Steam install logs**: Added a conservative `windows.steam-install-logs` rule for Steam's install-root `logs` directory, based on Valve's Steamworks debugging docs, plus planner, CLI, README, and rule-authoring coverage. Re-verified with `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (140 passing tests).
* **Steam install depot cache**: Added a conservative `windows.steam-install-depot-cache` rule for Steam's install-root `depotcache` directory, based on Valve's Steamworks documentation, plus planner, CLI, README, and rule-authoring coverage. Re-verified with `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace`.
* **Steam target resolution simplification**: Collapsed the Steam install/library target resolution branches in `rebecca-core/src/discovery.rs` into a shared helper, keeping the safe-relative validation and skip behavior intact. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-core --test discovery -p rebecca-core --test planner`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (142 passing tests).
* **Builtin catalog loader simplification**: Replaced the long `rebecca-rules` builtin TOML include list with a small macro-backed declarative list, keeping rule order and validation unchanged. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-rules`, and `cargo nextest run --workspace` (142 passing tests). This followed the earlier `aa038bd` test-helper refactor in the same iteration.
* **Steam registry source table simplification**: Replaced the `rebecca-windows` Steam registry source function with a static ordered constant table, keeping the discovery precedence and tests unchanged. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-windows --test recycle_bin`, `cargo nextest run -p rebecca-core --test discovery -p rebecca-core --test planner`, and `cargo nextest run --workspace` (142 passing tests).
* **Steam libraryfolders candidate tightening**: Tightened the Steam installation libraryfolders reader by making the candidate paths declarative and isolating the file-read helper, while preserving the existing fallback and error behavior. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-cli --test info -p rebecca-cli --test cli_output -p rebecca-cli --test cli_clean`, `cargo nextest run -p rebecca-core --test discovery -p rebecca-core --test planner`, and `cargo nextest run --workspace` (142 passing tests).
* **Steam wrapper removal**: Removed the redundant `rebecca-windows` Steam installation wrapper and routed Windows discovery directly through `SteamInstallation::from_install_path_best_effort`, then updated the Windows tests to use the core helper directly. Re-verified with `cargo fmt --all` and `cargo nextest run -p rebecca-windows --test recycle_bin -p rebecca-core --test discovery -p rebecca-core --test planner -p rebecca-cli --test info -p rebecca-cli --test cli_output -p rebecca-cli --test cli_clean` (80 passing tests in the targeted run).
* **Steam relative-target helper simplification**: Folded the shared Steam relative-target resolution path inside `rebecca-core::discovery` into a small scope enum plus direct iterator branches, keeping install-only and install-plus-library behavior unchanged. Re-verified with `cargo fmt --all` and `cargo nextest run -p rebecca-core --test discovery -p rebecca-core --test planner -p rebecca-cli --test info -p rebecca-cli --test cli_clean` (72 passing tests in the targeted run).
* **Steam rule baseline alignment**: Aligned the Steam built-in rule coverage baseline by adding `windows.steam-install-logs` to the catalog coverage test, keeping the recorded rule list in sync with the shipped catalog. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-rules -p rebecca-core --test discovery -p rebecca-core --test planner -p rebecca-cli --test cli_scan -p rebecca-cli --test scan`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (142 passing tests).
* **Steam test refactor and docs alignment**: Collapsed the Steam planner and CLI regression coverage into table-driven tests, keeping the same coverage while reducing the repeated case-by-case assertions. Refreshed the README, `docs/rule-authoring.md`, and engineering memory to keep the Steam catalog boundary wording explicit. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-core --test planner -p rebecca-cli --test cli_clean -p rebecca-cli --test cli_scan`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (132 passing tests).
* **Steam avatar cache**: Added a conservative `windows.steam-install-avatar-cache` rule for Steam's install-root `config\\avatarcache`, plus planner, CLI, README, and rule-authoring coverage. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-rules -p rebecca-core --test planner -p rebecca-cli --test cli_clean -p rebecca-cli --test cli_scan -p rebecca-cli --test scan`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (132 passing tests).
* **Steam test deepening**: Moved Steam rule cases and builtin rule expectations into repo-level shared test helpers so the CLI and core regression suites consume one Steam fixture table instead of repeating the same arrays. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-core --test planner -p rebecca-cli --test scan -p rebecca-cli --test cli_scan -p rebecca-cli --test cli_clean`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (132 passing tests).
* **Steam install stats cache**: Added a conservative `windows.steam-install-stats-cache` rule for Steam's install-root `appcache\\stats`, plus catalog, shared Steam test data, README, and rule-authoring coverage. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-rules -p rebecca-core --test planner -p rebecca-cli --test scan -p rebecca-cli --test cli_scan -p rebecca-cli --test cli_clean`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (132 passing tests).
* **Steam install appinfo cache**: Added a conservative `windows.steam-install-appinfo-cache` rule for Steam's install-root `appcache\\appinfo.vdf`, and extended the shared Steam test helper to support file targets so the new rule can be covered alongside the existing directory rules. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-rules -p rebecca-core --test planner -p rebecca-cli --test scan -p rebecca-cli --test cli_scan -p rebecca-cli --test cli_clean`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (132 passing tests).
* **Steam helper borrowing simplification**: Switched the shared Steam fixture helper and its CLI/core callers to borrowed cases instead of copied values, which keeps the table-driven Steam tests simpler without changing their assertions or fixture layout.
* **Steam install localization and package info caches**: Added conservative `windows.steam-install-localization-cache` and `windows.steam-install-packageinfo-cache` rules for the Steam install-root `appcache\\localization.vdf` and `appcache\\packageinfo.vdf` files, then extended the shared Steam fixture table, builtin catalog loader, README, and rule-authoring guidance to keep the new file rules visible and auditable. Re-verified with `cargo fmt --all`, `cargo nextest run -p rebecca-rules -p rebecca-core --test planner -p rebecca-cli --test scan -p rebecca-cli --test cli_scan -p rebecca-cli --test cli_clean`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo nextest run --workspace` (132 passing tests).
* **Configuration/local-state contract doc**: Added `docs/configuration.md` as the canonical contract for config schema v1, path precedence, local-state lifecycle ownership, cache purge boundaries, privacy, and migration rules. Linked it from README and ADR 0008, tightened rule-authoring guidance so built-in rules do not target Rebecca-owned storage, and re-verified with `cargo nextest run -p rebecca-core config`, `cargo nextest run -p rebecca-cli --test cli_output`, and `cargo nextest run -p rebecca-cli --test cli_cache`.
